services:
  # Static web frontend (Expo web export)
  - type: web
    name: aiwardrobe-web
    env: static
    buildCommand: npm install && npx expo export --platform web
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # Express API backend
  - type: web
    name: aiwardrobe-api
    runtime: node
    region: oregon
    plan: starter
    rootDir: api
    buildCommand: npm install
    startCommand: node index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "10000"
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: HF_TOKEN
        sync: false
      - key: REPLICATE_API_TOKEN
        sync: false
      - key: ALLOWED_ORIGINS
        value: "https://aiwardrobe-web.onrender.com"
