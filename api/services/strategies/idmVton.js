/**
 * IDM-VTON via Replicate strategy stub.
 * Full implementation should be added when IDM-VTON integration is needed.
 */
export async function idmVtonRender() {
  throw new Error('IDM-VTON strategy not yet implemented');
}
