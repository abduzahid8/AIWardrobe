// Re-export the real AI Try-On screen (Mobile-VTON powered).
// The RootNavigator registers this path — we forward to the full feature implementation.
export { default } from '../features/try-on/AITryOnScreen';
