/**
 * IDM-VTON Replicate client stub.
 * Full implementation should be added when IDM-VTON integration is needed.
 */
export async function checkIdmVtonHealth() {
  return { healthy: false, error: 'IDM-VTON client not yet implemented' };
}
